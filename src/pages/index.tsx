import React from 'react';
import { DashBoard } from 'Components/DashBoard';

export default function () {
  // get all groups in select
  // get all dashboards in select
  return <DashBoard id={1} />;
}
